import os

# Prefijo de comandos
PREFIJO = "r!"

# Color usado en los embeds
COLOR_CLUB = 0x2b6cb0

# Token del bot (variable de entorno en Railway, nunca en el código)
TOKEN = os.getenv("DISCORD_TOKEN")
